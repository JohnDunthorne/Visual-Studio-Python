# numbers = [1, 2]
# print(numbers[3])

# this will return an error as there is nothing at index 3

age = int(input("age: "))

# with the above if the user types something other than a 'int' it will cause an exception

# as a programmer you need to handle exceptions so the program does not crash
